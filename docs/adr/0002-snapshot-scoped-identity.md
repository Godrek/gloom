# Scope identity to immutable program snapshots

Program-entity identity is stable within an immutable program snapshot.
Relationships between entities or manifestations across observation contexts
and revisions are evidence-backed correspondence claims, not identity inferred
from matching names or similar bodies. This makes rename and refactor analysis
more explicit, but avoids irreversible false merges involving local symbols,
generated code, clones, and transformed artifacts.
